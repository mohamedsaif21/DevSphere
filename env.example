# Copy to .env.local for local dev. Add the same keys in Vercel → Project → Settings → Environment Variables (Production + Preview).

# Compiler run (Judge0 via RapidAPI)
RAPIDAPI_KEY=
JUDGE0_URL=https://judge029.p.rapidapi.com

# AI debug (Gemini)
GEMINI_API_KEY=
# GEMINI_MODEL=
